# === FRONTEND (React) ===
# Node.js dependencies, build output, and local IDE/env files - KEEP IGNORED
/DPA/node_modules/
/DPA/dist/
/DPA/build/
/DPA/.env
/DPA/.idea/
/DPA/.vscode/

# === C# BACKEND (.NET) ===
# Compiled binaries, intermediate objects, and local IDE files - KEEP IGNORED
/WILMA_Backend/bin/
/WILMA_Backend/obj/
/WILMA_Backend/.vs/
/WILMA_Backend/.idea/
# Die C# Backend SQLite DB (WilmaDB.db) wird bei Bedarf neu generiert.
# Sie wird hier NICHT explizit ignoriert, damit auch die KI-Datenbank (forecast.db)
# mit erfasst wird, da wir keine allgemeine *.db Ignorierung haben.

# === PYTHON KI-BACKEND (FastAPI) ===
# Python virtual environment (sehr groß!) - KEEP IGNORED
# Dieses Verzeichnis MUSS ignoriert werden, um die "Files too large" Fehler zu vermeiden.
/WILMA_Fullstack/WaterflowForecast/venv/
/WILMA_Fullstack/WaterflowForecast/__pycache__/
/WILMA_Fullstack/WaterflowForecast/*.pyc
/WILMA_Fullstack/WaterflowForecast/*.pyo
/WILMA_Fullstack/WaterflowForecast/*.pyd

# Generierte Ausgabedateien und Benutzer-Uploads - KEEP IGNORED
# Der 'results' Ordner enthält dynamisch generierte Plots und Vergleichs-CSVs.
# Er MUSS ignoriert werden.
/WILMA_Fullstack/WaterflowForecast/results/
# Der 'uploads' Ordner enthält vom Benutzer hochgeladene Dateien.
/WILMA_Fullstack/WaterflowForecast/uploads/

# === GENERAL / ROOT LEVEL & DYNAMIC FILES ===
# Allgemeine Logs und temporäre Systemdateien - KEEP IGNORED
*.log
*.tmp
*.DS_Store
*.user
*.suo
*.swp

# SQLite runtime files (dynamisch generiert während des Betriebs) - KEEP IGNORED
*.db-shm
*.db-wal

# === DATEIEN, DIE IM REPO SEIN MÜSSEN (NICHT MEHR IGNORIERT) ===
# Wenn hier kein Eintrag für eine Datei ist, wird Git sie standardmäßig verfolgen,
# solange sie nicht in einem der ignorierten Ordner liegt.

# appsettings.json: Essentiell für das C# Backend. Ist jetzt NICHT ignoriert.
# forecast.db: Die initial befüllte KI-Datenbank. Ist jetzt NICHT ignoriert.
# *.csv in 'data/synthetic/': Die initialen Daten-CSVs. Sind jetzt NICHT ignoriert (da *.csv entfernt).
# *.png, *.jpg etc.: Statische Bild-Assets (Logo, Flaggen). Sind jetzt NICHT ignoriert.

# === SPEZIFISCHE LOKALE UPLOAD-ORDNER ===
# Ordner für hochgeladene Profilbilder (vom C# Backend genutzt) - KEEP IGNORED
# Dieser Ordner wird dynamisch erstellt und enthält Benutzer-Uploads, sollte daher NICHT im Repo sein.
/WILMA_Backend/wwwroot/profile-images/

# === OPTIONAL: Sonstiges ===
# Temporäre Backup-Dateien
*.bak
*.old
